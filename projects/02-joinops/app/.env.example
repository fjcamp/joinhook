DATABASE_URL=
JOINOPS_TENANT_ID=
JOINOPS_ENV=development
JOINOPS_SESSION_SECRET=

# Never commit real secrets. Production values belong in the cloud provider secret store.
# DATABASE_URL and JOINOPS_TENANT_ID are server-side only; never prefix them with NEXT_PUBLIC_.
